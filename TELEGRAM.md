# اتصال تورکریپتور به تلگرام (مینی‌اپ)

نسخه وب (`docs/`) کاملاً سمت کاربره — رمز و پیام هرگز از دستگاه کسی خارج نمی‌شه،
پس اتصالش به تلگرام امنه.

## قدم ۱: ساخت بات

1. تو تلگرام به [@BotFather](https://t.me/BotFather) پیام بده
2. دستور `/newbot` رو بزن
3. یه اسم بده (مثلاً `Turcryptor`) و یه یوزرنیم که به `bot` ختم بشه (مثلاً `turcryptor_bot`)
4. BotFather یه **token** بهت می‌ده — اینو نگه دار (برای قدم اختیاری ۳ لازمه)

## قدم ۲: وصل کردن مینی‌اپ به لینک Pages

بعد از اینکه لینک GitHub Pages فعال شد (مثل `https://USERNAME.github.io/turcryptor/`):

1. تو BotFather دستور `/newapp` رو بزن (یا: `/mybots` → باتت → **Bot Settings** → **Menu Button**)
2. باتت رو انتخاب کن
3. آدرس لینک Pages رو به‌عنوان Web App URL بده
4. اسم، توضیحات و عکس آپلودش کن

از این به بعد دوستات تو چت، دکمه «تورکریپتور» رو کنار باکس پیام می‌بینن و با یه تپ
اپ باز می‌شه.

## قدم ۳ (اختیاری): مدیریت با API

اگه خواستی من (یا اسکریپتی) بات رو با API تنظیم کنه، توکن BotFather رو بده تا
با متد `setChatMenuButton` لینک مینی‌اپ ست بشه. توکن رو تو هیچ فایلی از پروژه
نذار — فقط تو پیام بهم بده یا تو یه متغیر محیطی.

## نکته برای دوستان ایران

`github.io` برای IP ایران گاهی محدوده — دوستات اگه تلگرام دارن، احتمالاً
پروکسی/VPN دارن و مشکلی نیست. اگه کسی نتونست باز کنه، بگو تا یه نسخه موازی روی
Cloudflare Pages (`pages.dev`) هم منتشر کنیم.
